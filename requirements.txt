streamlit==1.39.0
pandas==2.2.2
openpyxl==3.1.5
requests==2.32.3
Pillow==10.4.0
# rembg and onnxruntime are optional heavy native deps that may fail on Streamlit Cloud
# If you need background removal in production, re-add the following and ensure
# the system packages in packages.txt are present or use a custom wheel:
# rembg==2.0.59
# onnxruntime==1.19.2
urllib3==2.2.2
